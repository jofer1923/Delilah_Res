openapi: 3.0.0
# Added by API Auto Mocking Plugin
servers:
  - description: SwaggerHub API Auto Mocking
    url: https://virtserver.swaggerhub.com/jofer1923/FERNANDO_BEDOYA_DELILAH_RESTO/1.0.0
info:
  description: Acamica final project of a restful API for a restaurant
  version: "1.0.0"
  title: DELILAH RESTO
  contact:
    email: jofer1923@gmail.com
  license:
    name: Apache 2.0
    url: "http://www.apache.org/licenses/LICENSE-2.0.html"
tags:
  - name: admin
    description: endpoint admin's crud
  - name: users
    description: endpoint users' crud
paths: admin
  /inventory:
    get:
      tags:
        - developers
      summary: searches inventory
      operationId: searchInventory
      description: |
        By passing in the appropriate options, you can search for
        available inventory in the system
      parameters:
        - in: query
          name: searchString
          description: pass an optional search string for looking up inventory
          required: false
          schema:
            type: string
        - in: query
          name: skip
          description: number of records to skip for pagination
          schema:
            type: integer
            format: int32
            minimum: 0
        - in: query
          name: limit
          description: maximum number of records to return
          schema:
            type: integer
            format: int32
            minimum: 0
            maximum: 50
      responses:
        "200":
          description: search results matching criteria
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryItem"
        "400":
          description: bad input parameter
    post:
      tags:
        - admins
      summary: adds an inventory item
      operationId: addInventory
      description: Adds an item to the system
      responses:
        "201":
          description: item created
        "400":
          description: "invalid input, object invalid"
        "409":
          description: an existing item already exists
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InventoryItem"
        description: Inventory item to add
components:
  schemas:
    userservice:
      type: object
      required:
        - User_Id
        - Nick_Name
        - User_Name
        - Mail
        - Contact 
        - PWD
      properties:
        User_Id:
          type: integer
          format: uuid
          example: 1          
        Nick_Name:
          type: string
          example: "jofer1923"
        User_Name:
          type: string          
          example: "Fernando Bedoya"
        Mail:
          type: string
          format: email
          example: "Jofer1923@gmail.com"
        Contact:
          type: string          
          example: "+01 22222222"
        PWD:
          type: string          
          example: "Coronavirus2020"        
    Manufacturer:
      required:
        - name
      properties:
        name:
          type: string
          example: ACME Corporation
        homePage:
          type: string
          format: url
          example: "https://www.acme-corp.com"
        phone:
          type: string
          example: 408-867-5309
      type: object
